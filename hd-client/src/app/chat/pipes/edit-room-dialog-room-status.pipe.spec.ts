import { EditRoomDialogRoomStatusPipe } from './edit-room-dialog-room-status.pipe';

describe('EditRoomDialogRoomStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new EditRoomDialogRoomStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
