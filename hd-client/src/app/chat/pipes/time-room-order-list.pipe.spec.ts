import { TimeRoomOrderListPipe } from './time-room-order-list.pipe';

describe('TimeRoomOrderListPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeRoomOrderListPipe();
    expect(pipe).toBeTruthy();
  });
});
