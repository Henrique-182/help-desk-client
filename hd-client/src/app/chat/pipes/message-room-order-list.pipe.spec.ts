import { MessageRoomOrderListPipe } from './message-room-order-list.pipe';

describe('MessageRoomOrderListPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageRoomOrderListPipe();
    expect(pipe).toBeTruthy();
  });
});
