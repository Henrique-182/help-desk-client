import { HeaderDialogUploadFilePipe } from './header-dialog-upload-file.pipe';

describe('HeaderDialogUploadFilePipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderDialogUploadFilePipe();
    expect(pipe).toBeTruthy();
  });
});
